# version: '3.10'

# services:
    

#   web:
#         build: .
#         # command: python manage.py runserver 0.0.0.0:8000

#         volumes:
#             - .:/app
#         ports:
#             - "8000:8000"
        
#         entrypoint: "/app/entrypoint.sh"
#         environment:
#             - DB_NAME=gestion_stock_db
#             - DB_USER=postgres
#             - DB_PASSWORD=************8
#             - DB_HOST=localhost
#             - DB_PORT=5432
#             - SECRET_KEY=****************
#             - EMAIL_HOST_PASSWORD=*******************


version: '3.10'

services:

  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: gestion_stock_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 2024
    ports:
      - "5432:5432"

  web:
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    entrypoint: "/app/entrypoint.sh"
    depends_on:
      - db
    environment:
      - DB_NAME=gestion_stock_db
      - DB_USER=postgres
      - DB_PASSWORD=2024
      - DB_HOST=db   # 👈 IMPORTANT
      - DB_PORT=5432
      - SECRET_KEY=django-insecure-w=jn2myz$8@@2xoz9^^qy8ml1(^=#5qjbxp_7bwkhhork_f2x#
      - EMAIL_HOST_PASSWORD=qffgcutvojhakxzq